# Third-Party Notices

Buffaly Agent Kit 1.0 is a GPL-3.0-only reference implementation.

The frozen runtime packages include components owned by Matt Furnari/Buffaly and redistributed as part of this local reference build:

- ProtoScript runtime assemblies
- Ontology runtime assemblies
- BasicUtilities.dll when required by the frozen runtime

NuGet dependencies are pinned in `Directory.Packages.props`.
