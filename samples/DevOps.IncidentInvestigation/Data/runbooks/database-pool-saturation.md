# Database pool saturation

Use this runbook when database pool utilization is elevated and request latency rises. Verify metrics, review recent configuration changes, and have an operator decide whether to scale pool capacity or roll back.
