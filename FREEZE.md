# Buffaly Agent Kit 1.0 Freeze Notes

- Target framework: `net10.0`.
- SDK pinned in `global.json`: `10.0.301`.
- Version line: Agent Kit 1.0, manifest schema version 1, event schema version 1.
- The source tree is public at `https://github.com/buffaly-ai/agentkit`.
- Release artifacts are generated only after clean restore, build, test, package-consumer, and inventory validation succeed.
