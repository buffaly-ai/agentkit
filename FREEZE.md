# Buffaly Agent Kit 1.0 Freeze Notes

- Intended target framework: net10.0.
- Local temporary target framework: net9.0 because `dotnet --list-sdks` reports 5.0.413, 8.0.406, and 9.0.300 only.
- SDK pinned in `global.json`: 9.0.300.
- Version line: Agent Kit 1.0, Manifest schema version 1, Event schema version 1.
- This tree is local build work only under `C:\dev\buffaly-ai\agentkit`.
- Do not push to a remote from this workspace.
